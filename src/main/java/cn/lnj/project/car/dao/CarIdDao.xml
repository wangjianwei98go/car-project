<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lnj.project.car.dao.CarIdDao">

    <resultMap id="infoResultMap" type="cn.lnj.project.car.data.carId"></resultMap>

    <!--根据id查询用户-->
    <select id="selectBycarid" resultMap="infoResultMap">
        SELECT * FROM carid WHERE carId = #{carid};
    </select>

</mapper>